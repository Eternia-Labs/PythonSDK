#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='sc-tasks-python-sdk',
      version='0.0.1',
      description='This package is for sctasks python SDK.',
      packages=find_packages(),
)
